

export function findMax(arr) {
    // ASSUMES AN ARRAY OF THINGS THAT CAN HAVE A MAX.  
    // EG NUMBERS
    return Math.max(...arr);
}


export function reverseArray(arr) {
    // REVERSE IT IN-PLACE
    arr.reverse();
    return arr;
}

